[package]
name = "runtime-bootstrap"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Composicao runtime/infra do Mini-Kernel RS"

[dependencies]
support-errors = { path = "../../support/support-errors" }
support-crypto = { path = "../../support/support-crypto" }
support-storage = { path = "../../support/support-storage" }
support-logging = { path = "../../support/support-logging" }
core-audit = { path = "../../core/core-audit" }
core-config = { path = "../../core/core-config" }
adapter-sqlite        = { path = "../adapter-sqlite" }
adapter-audit-sqlite  = { path = "../adapter-audit-sqlite" }
serde_json            = { workspace = true }
zeroize               = { workspace = true }

[dev-dependencies]
serde_json.workspace = true
tempfile.workspace = true
